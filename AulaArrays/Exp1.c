#include <stdio.h>

int main(){

    // Isso cria um array de inteiros com 5 elementos
     int numeros[5]; 

     numeros[0] = 10; // Coloca o número 10 na primeira caixa (índice 0)
     numeros[1] = 20; // Coloca o número 20 na segunda caixa (índice 1)

}